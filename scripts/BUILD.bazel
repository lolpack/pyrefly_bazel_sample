load("@rules_python//python:defs.bzl", "py_binary")

py_binary(
    name = "run_report",
    srcs = ["run_report.py"],
    deps = [
        "//click:click_lib",
        "//plugins:analyzer",
    ],
)
